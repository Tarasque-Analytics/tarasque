import Macro from "../pages/Macro";

// No loader: /macro is scaffolding with no data layer yet (see pages/Macro.tsx). When a future
// /api/macro endpoint lands, add a non-fatal loader here mirroring routes/ticker.tsx.
export default Macro;
