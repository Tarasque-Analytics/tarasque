# Requires Python >= 3.10 (python-dotenv 1.2.x needs 3.10+).
# On Python 3.8/3.9, pip fails with a misleading "Could not find a version
# that satisfies the requirement python-dotenv==1.2.2" — it's a Python version
# mismatch, not a missing release. Install/run with Python 3.10+ (e.g. `py -3.11`).
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6
python-dotenv==1.2.2
supabase

# TODO: PostgreSQL Integration
# psycopg2-binary==2.9.9
# sqlalchemy==2.0.23
# pydantic-sqlalchemy==0.0.14

# Optional: For caching and performance
# redis==5.0.1
