# Tarasque Model Pipeline — Python dependencies
# Install with: pip install -r requirements.txt
#
# Requires Python 3.9+
# WRDS requires a separate institutional login (wrds.wharton.upenn.edu)

# ── Core data / numerics ─────────────────────────────────────────────────────
numpy>=1.24
pandas>=2.0
scipy>=1.11

# ── Data storage ─────────────────────────────────────────────────────────────
pyarrow>=14.0          # Parquet read/write

# ── WRDS access ───────────────────────────────────────────────────────────────
wrds                   # Wharton WRDS PostgreSQL client

# ── Machine learning ─────────────────────────────────────────────────────────
scikit-learn>=1.3
xgboost>=2.0

# ── Volatility / GARCH ───────────────────────────────────────────────────────
arch>=6.0              # GARCH(1,1) with SkewT distribution

# ── Explainability ────────────────────────────────────────────────────────────
shap>=0.44

# ── Market data (continuation layer) ─────────────────────────────────────────
alpaca-py>=0.20        # Alpaca SDK v2 (NOT alpaca-trade-api, that's deprecated)
yfinance>=0.2.36       # Treasury rate proxies for FRED continuation

# ── Utilities ────────────────────────────────────────────────────────────────
python-dotenv>=1.0
python-dateutil>=2.8
